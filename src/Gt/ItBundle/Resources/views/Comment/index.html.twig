{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Comment list</h1>
<table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>Id</th>
                <th>Blog_id</th>
                <th>User_id</th>
                <th>Comment</th>
                <th>Comment threads</th>
                <th>Date</th>
                 <th>userName</th>
                <th>Anono.Name</th>
                <th>Mail</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_comment_show', { 'id': entity.id ,'item': entity.blog_id}) }}">{{ entity.id }}</a></td>
                <td>{{ entity.blog_id }}</td>
                <td>{{ entity.user_id }}</td>
                <td>{{ entity.comment }}</td>
                  <td> {% set var = 0 %}{% for item in th %}
     
                       {% if item.thread |trans == entity.id|trans  %}
                              {% set var = var + 1 %}      
                        {% endif %}
                      
                       
                 

                        

                      
                     
                  {% endfor %}  <a href="{{path('admin_threads',{'comment_id':entity.id})}}">{{ var   }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.user != null %}
                {{ entity.user.username }}{% endif %}</td>
                 <td>{{ entity.name }}</td>
                <td>{{ entity.mail }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_comment_show', { 'id': entity.id ,'item': entity.blog_id}) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_comment_edit', { 'id': entity.id ,'item': entity.blog_id}) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     
    {% endblock %}
